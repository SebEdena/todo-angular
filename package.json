{
  "name": "todo-a16",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "lib/dist"
  ],
  "scripts": {
    "setup:lib": "(cd lib; npm i; npm run build)",
    "setup:web": "(cd frontend; npm i)",
    "setup:api": "(cd backend; npm i)",
    "setup": "npm i; npm-run-all --parallel setup:*"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
