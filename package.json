{
  "name": "todo-a16",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "lib/dist"
  ],
  "scripts": {
    "init-lib": "(cd lib; npm i; npm run build)",
    "install-all": "npm run init-lib && npm install --workspaces --if-present",
    "build-all": "npm run init-lib && npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
